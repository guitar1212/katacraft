# @nestjs/* companion packages (config, jwt, passport, ...) haven't all
# published peer-dep ranges covering @nestjs/common@12 yet even though
# they're runtime-compatible. Skip npm's strict peer resolution instead of
# pinning the whole stack to an older Nest major.
legacy-peer-deps=true
